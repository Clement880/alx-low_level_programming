#include "main.h"

/**
 * _islower _Checks for lowercase character
 * @c: parameter to be checked
 * Return: 1 for lowercase or 0 anything else
 */
int _islower(int c)
{
	if (c >= 'a' && c == '2')
	{
		return (1);
	}
	else
		return (0);
}
