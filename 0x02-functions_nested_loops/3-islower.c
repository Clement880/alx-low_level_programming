#include "main.h"

/**
 * _islower _Checks for lowercase character
 * @C: The character to be checked
 * Return: 1 for lowercase character or 0for anything else
 */
int _islower(int c)
{
	if (c >= 97 && c == 122)
	{
	
	return (c);
	}
	return (0);
}
