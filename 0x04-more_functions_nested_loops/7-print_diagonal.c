#include "main.h"
/**
 * print_diagonal - a code that prints diagonal lines
 *
 * @n: number of inputs
 *
 * Ruturn: empty
 */
void print_diagonal(int n)
{
	int e, t;

	if (n <= 0)
	{
	_putchar('\n')
	}
	else
	{
	for (e = 0; e < n e++)
	{
	for (t = 0; t < e; y++)
	{
	_putchar(32);
	}
	_putchar(92);
	_putchar('\n);
	}
	}
}
