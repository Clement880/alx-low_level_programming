#include <stdio.h>
/**
 * main - Entry start point
 *
 * Return: Everytime (0) when successful
 *
 */
int main(void)
{
	char b;

	for (b = 'a'; b <= 'z'; b++)
	putchar(b);
	putchar('\n');
	return 0;
}
