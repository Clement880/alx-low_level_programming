#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
#include "main.h"
/**
 * _strlen - returns the length of the string
 * @s: the string
 * Return: length
 */
int _strlen(char *s)
{
    return strlen(s);
}
